//export
/*
let paymentAmount = 5;
const card = [];

const addToCard = (product, quantity = 1) => {
  card.push({ product, quantity });
  console.log(`Added ${quantity} ${product} to card`);
};

const amount = 4;
const name = 'door';
export { addToCard, amount, name, card };
export default paymentAmount;
*/

// console.log('start fetching users');
// await fetch('https://jsonplaceholder.typicode.com/posts');
// console.log('end fetching users');

export default 5;
export const card = [];
